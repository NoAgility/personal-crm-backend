version: '3.8'
services:
  backend-service:
    container_name: backend-service
    build:
      context: backend-service
    restart: always
    ports:
      - 80:8080
    environment:
      TEST: test
    healthcheck:
      test: "exit 0"
